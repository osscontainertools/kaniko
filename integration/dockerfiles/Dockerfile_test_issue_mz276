FROM busybox AS first

ARG TARGETOS
ARG TARGETARCH
RUN test "${TARGETOS}-${TARGETARCH}" = "linux-amd64"

FROM first AS second

ARG TARGETOS=linux
ARG TARGETARCH=x86_64
RUN test "${TARGETOS}-${TARGETARCH}" = "linux-x86_64"
