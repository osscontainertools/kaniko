version: "2"

linters:
  default: standard
  enable:
    - goconst
    - misspell
    - unconvert
    - unparam
    - copyloopvar
    - makezero
    - wastedassign

formatters:
  enable:
    - gci
    - gofmt
    - goimports
    - gofumpt
    - swaggo
